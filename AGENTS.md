# AI 新职业雷达规则

## 目标

发现 AI 技术正在催生的职业与工作任务组合，为良的求职和能力投资提供早期、可核验的方向判断。

本项目不等同于岗位抓取器：BossHunter 负责具体岗位，新职业雷达负责判断职业结构变化。

## 证据原则

- 重要结论必须带来源、发布日期、观察日期和适用地区。
- 优先级：政府职业分类与标准 > 招聘平台或劳动力市场的一手统计 > 企业官方招聘页 > 研究机构报告 > 媒体与社交讨论。
- 一个新职位名不等于新职业。必须检查核心任务是否稳定、是否被多家独立雇主重复采购、需求是否持续。
- 官方认定不等于招聘已经放量；“制度状态”和“市场需求阶段”必须分开记录。
- 数据来自海外时明确标注地区，不直接外推中国或杭州。
- 只保存必要摘要和链接，不整段复制招聘 JD，不绕过登录、验证码、robots 或平台限制。
- 数字必须有来源；无法核验的数字不写，估算必须显式标注假设。
- 每个正向判断至少记录一个反例、替代解释或失效条件。

## 分类口径

每个职业簇同时记录四项：

1. `novelty_zone`：`frontier` / `expanding` / `forming` / `baseline` / `cooling`
2. `institutional_status`：`official` / `industry_named` / `analyst_derived`
3. `demand_stage`：`seed` / `forming` / `scaling` / `mature` / `weakening`
4. `confidence`：`low` / `medium` / `high`
5. `fit_for_liang`：`direct` / `stretch` / `observe` / `avoid`

同一证据快照提供两种展示排序：`市场前沿版` 先看 `novelty_zone` 与最近 6—12 个月的跨雇主扩散，再看证据强度；`良的适配版` 使用单独记录的适配顺序与进入理由。两版只改变排序、说明和筛选，不改变职业的事实、层级或证据。官方认定只证明制度身份，不证明仍是新职业，也不因此获得市场前沿版首页优先级。

- `frontier`：近 6—12 个月出现的新任务簇或新细分，样本仍少，必须明确可能不成职业。
- `expanding`：至少两家独立雇主重复采购，或同一雇主已扩展到多个行业、地区与层级。
- `forming`：任务已稳定出现，但跨雇主或持续性证据尚不足。
- `baseline`：需求真实但已成常规岗位或通用能力，不再算新发现。
- `cooling`：独立头衔正在被其他岗位吸收、低端供给商品化，或证据明显弱化。

第一期不设置伪精确综合分。证据积累后才能定义并冻结评分权重。

## 良的匹配边界

- 已确认优势：临床医学背景、AI 应用实践、产品与流程意识、模型评测和内容质控经验、可验证项目工作流。
- 不把 AI 辅助完成项目夸大为独立算法研发或全栈工程能力。
- 优先关注医学领域评测、知识质量、AI 应用测试、Agent 工作流、产品运营与落地支持。
- 泛数据标注、纯提示词职位、无固定薪资的拉新销售和名称炫目但任务模糊的岗位默认降级。

## 更新流程

1. 登记信源并保存日期。
2. 提取职位别名、核心任务、工具、行业、经验与学历要求。
3. 合并同义职位，形成任务簇。
4. 核对跨公司重复、增长持续性、薪资与进入门槛。
5. 写清“为什么现在算新”、最近信号日期、事实、推断、反证和对良的行动建议。
6. 更新 `watchlist.md`，再发布一期报告。

## 目录与命名

- `README.md`：目标、状态和入口。
- `watchlist.md`：当前职业观察池，只放最新状态。
- `sources/registry.md`：长期信源登记与使用边界。
- `reports/YYYY-MM-DD-第N期.md`：有日期的阶段报告，发布后不静默改写结论；纠错追加说明。
- `site/`：AI 新职业雷达网站源码，只展示已核验的派生结论和来源链接。
- `site/lib/radar-data.ts`：网站当前快照；只允许从 `watchlist.md` 和已发布报告同步，不反向改写研究结论。

不创建无归属的临时文件。过时观察从 `watchlist.md` 移出时，在对应报告保留历史依据。

## 网站原型规则

- 单页工作台首屏直接提供搜索、新颖度筛选和有效职业簇，不做营销型首页；默认优先显示前沿、扩散和成型职业。
- 首屏提供 `市场前沿版 / 良的适配版` 切换；前者默认突出 FDE 等近期扩散职业，后者默认突出良能凭临床、AI 应用、产品和评测经验切入的职业。
- 两个版本和手动刷新必须位于同一个独立站内。哨塔只负责启停并打开这个站，不得复制一套“哨塔版”职业雷达。
- 页面必须显式显示“人工研究版 / 非实时”、观察日期、地区边界和证据等级，不能伪装成实时招聘数据。
- 每个职业簇至少展示新颖度层级、最近信号、制度状态、需求阶段、置信度、对良的匹配度、核心判断和主要证据链接。
- 研究更新顺序固定为：登记信源 → 更新报告与 `watchlist.md` → 同步网站快照 → 构建验证。
- 不接数据库、账号、Cookie、招聘平台登录态、通知或 BossHunter 原始岗位、简历与聊天数据。
- 本地开发站可提供手动“一键刷新”：只访问代码内固定白名单的一手公开信源，只生成带检查时间、成功失败和候选命中的本地状态文件，不得自动改写 `watchlist.md`、报告或 `site/lib/radar-data.ts`，不得把候选命中伪装成已核验职业结论。
- 刷新接口只在回环地址的本地开发服务可用；不得接收任意 URL、路径、命令、请求头或凭证，不注册定时任务，不在静态托管环境声称可持久更新。
- 本地原型可以直接实现和验证；公开部署、域名、定时任务及第三方连接仍需良单独确认。2026-08-29 已明确授权将 `fengziliang43-cmyk/ai-career-radar` 源码仓库设为公有；该授权只涵盖当前源码、研究文档和提交历史，不包含网站部署、域名、定时任务或外部数据连接。
- 站点生成文件、依赖缓存和构建产物留在 `site/` 内；临时产物不进入项目根目录。

## 安全与发布

- 不保存账号、Cookie、Token、个人联系方式、患者资料或私密求职沟通。
- 当前只做本地研究文档；网站、定时任务、公开部署、域名和通知均需良另行确认。
- 若未来做网站，默认只公开派生趋势和来源链接，不公开原始招聘数据库、简历或聊天记录。
